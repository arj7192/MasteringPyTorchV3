# Mastering PyTorch, 3rd Edition — base requirements
# Floors pinned to latest stable as of Aug 2026.
# Install: python -m venv .venv && source .venv/bin/activate && pip install -U pip && pip install -r requirements.txt
# GPU (CUDA): follow https://pytorch.org/get-started/locally/ then reinstall torch/torchvision/torchaudio for your CUDA.

# --- Core (PyTorch 2.13 stack) ---
torch>=2.13.0
torchvision>=0.28.0
torchaudio>=2.11.0
numpy>=1.26,<2.3
pandas>=2.2
scipy>=1.15
matplotlib>=3.10
pillow>=11.0

# --- Notebooks and tooling ---
jupyter>=1.1
ipykernel>=6.29
ipywidgets>=8.1
nbconvert>=7.16
nbclient>=0.10
tqdm>=4.67

# --- Hugging Face stack (Ch 4, 5, 7, 8, 10, 11, 13, 19) ---
transformers>=4.52
datasets>=3.6
tokenizers>=0.21
accelerate>=1.7
peft>=0.15
trl>=0.18
diffusers>=0.33
optimum>=1.25
huggingface-hub>=0.33

# --- Vision and multimodal (Ch 4, 5) ---
timm>=1.0.15
open-clip-torch>=2.32

# --- Graph (Ch 6) ---
torch-geometric>=2.6

# --- RL (Ch 11) ---
gymnasium>=1.1
ale-py>=0.10

# --- Production / serving (Ch 12, 13) ---
onnx>=1.18
onnxruntime>=1.22
# torchserve, vllm, deepspeed: install per-notebook (platform / GPU specific)

# --- Edge (Ch 14) ---
# executorch: install per-platform; see Chapter14/README.md

# --- Recsys (Ch 18) ---
# torchrec: often platform-specific; install when working on Ch 18

# --- Rapid prototyping (Ch 15) ---
fastai>=2.8
pytorch-lightning>=2.5
torch-tb-profiler>=0.4

# --- AutoML (Ch 16) ---
optuna>=4.2
# autogluon / autokeras: heavy; install when working on Ch 16

# --- Explainability (Ch 17) ---
captum>=0.8

# --- Responsible / Efficient AI (distributed across Ch 6–14) ---
opacus>=1.5
fairlearn>=0.12
