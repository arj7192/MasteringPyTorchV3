# Mastering PyTorch, 3rd Edition — base requirements
# Anchored on PyTorch 2.x. Pins will be tightened per-chapter as notebooks are finalized.

# --- Core ---
torch>=2.4
torchvision>=0.19
torchaudio>=2.4
numpy>=1.26
pandas>=2.2
scipy>=1.13
matplotlib>=3.8
pillow>=10.3

# --- Notebooks and tooling ---
jupyter>=1.0
ipykernel>=6.29
ipywidgets>=8.1
tqdm>=4.66

# --- Hugging Face stack (used in Ch 5, 6, 8, 9, 11, 12, 14, 20) ---
transformers>=4.44
datasets>=2.20
tokenizers>=0.19
accelerate>=0.33
peft>=0.12
trl>=0.10
diffusers>=0.30
optimum>=1.21
huggingface-hub>=0.24

# --- Vision and multimodal (Ch 5, 6) ---
timm>=1.0
open-clip-torch>=2.26

# --- Graph (Ch 7) ---
torch-geometric>=2.6

# --- RL (Ch 12) ---
gymnasium>=0.29
ale-py>=0.10

# --- Production / serving (Ch 13, 14) ---
torchserve>=0.12
onnx>=1.16
onnxruntime>=1.18
# vllm and deepspeed pulled in per-notebook (Linux/GPU only)

# --- Edge (Ch 15) ---
# executorch installed per-platform; see Chapter15/README.md

# --- Recsys (Ch 19) ---
torchrec>=0.8

# --- Rapid prototyping (Ch 16) ---
fastai>=2.7
pytorch-lightning>=2.3
torch-tb-profiler>=0.4

# --- AutoML (Ch 17) ---
optuna>=3.6
autogluon>=1.1
autokeras>=2.0

# --- Explainability (Ch 18) ---
captum>=0.7

# --- Responsible AI (Ch 21) ---
opacus>=1.5             # differential privacy
fairlearn>=0.10         # bias / fairness
aif360>=0.6             # bias / fairness alt
